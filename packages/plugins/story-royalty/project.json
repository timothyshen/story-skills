{
  "name": "story-royalty",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/plugins/story-royalty",
  "projectType": "library",
  "tags": ["type:plugin", "scope:story-protocol"],
  "targets": {
    "lint-markdown": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx markdownlint-cli2 'packages/plugins/story-royalty/**/*.md'"
      }
    },
    "validate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/validate-plugin.cjs packages/plugins/story-royalty"
      }
    }
  }
}
